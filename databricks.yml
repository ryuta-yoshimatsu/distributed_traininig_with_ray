# This is a Databricks asset bundle definition for distributed_traininig_with_ray.
# See https://docs.databricks.com/dev-tools/bundles/index.html for documentation.
bundle:
  name: distributed_traininig_with_ray
  uuid: d6271be5-d62c-4ef8-9a55-bebfba4cfa29

include:
  - resources/*.yml
  - resources/*/*.yml

targets:
  dev:
    # The default target uses 'mode: development' to create a development copy.
    # - Deployed resources get prefixed with '[dev my_user_name]'
    # - Any job schedules and triggers are paused by default.
    # See also https://docs.databricks.com/dev-tools/bundles/deployment-modes.html.
    mode: development
    default: true
    workspace:
      host: https://e2-demo-field-eng.cloud.databricks.com

  prod:
    mode: production
    workspace:
      host: https://e2-demo-field-eng.cloud.databricks.com
      # We explicitly deploy to /Workspace/Users/ryuta.yoshimatsu@databricks.com to make sure we only have a single copy.
      root_path: /Workspace/Users/ryuta.yoshimatsu@databricks.com/.bundle/${bundle.name}/${bundle.target}
    permissions:
      - user_name: ryuta.yoshimatsu@databricks.com
        level: CAN_MANAGE
